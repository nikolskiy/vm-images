[project]
name = "vm-images"
version = "0.1.0"
description = "Packer and Ansible scripts for building developer VM images."
requires-python = ">=3.12"
dependencies = [
    "ansible",
    "ansible-lint",
    "sh>=2.2.2",
    "typer",
]


[tool.setuptools.packages.find]
where = ["src"]
